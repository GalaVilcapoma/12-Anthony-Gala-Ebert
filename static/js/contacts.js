// Animaciones para tabla de contactos
// (Se puede ampliar con funcionalidades de filtrado o búsqueda)

document.addEventListener('DOMContentLoaded', function () {
    // Ejemplo: animación al cargar la tabla
    const table = document.querySelector('table');
    if (table) {
        table.classList.add('table-anim');
    }
});
